{
  "name": "javaprojects",
  "version": "0.0.3",
  "description": "java project docs site build with vitepress",
  "dependencies": {
    "esbuild": ">=0.27.2"
  },
  "devDependencies": {
    "esbuild": ">=0.27.2",
    "vitepress": "^1.6.4"
  },
  "scripts": {
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  }
}